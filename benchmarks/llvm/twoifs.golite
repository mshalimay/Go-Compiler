var Node int;



func printDepthTree (curr int)  {
    var temp int;
	if( curr != 0 ) {
        curr = 0;
		if( curr > 0) {
			printDepthTree(curr);
		}

        temp = Node;
		printf("%d",temp);

	}
}

func main(){
    return;
}